[package]
name = "dataframes"
version = "0.1.0"
description = "Pandas-like tabular data for Maxima (SBCL only)"
license = "MIT"
entry = "dataframes.mac"
doc = "doc/dataframes.md"

[lisp]
quicklisp_systems = ["magicl", "trivial-garbage", "alexandria"]

[test]
files = ["rtest_dataframes.mac"]
